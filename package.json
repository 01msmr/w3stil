{
  "name": "w3stil",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Reduziertes Lese-Layout als UserCSS für Stylus",
  "scripts": {
    "build": "node build.mjs",
    "dev": "node build.mjs --serve",
    "check": "node tools/check.mjs",
    "lint": "stylelint \"src/**/*.css\"",
    "deploy": "node build.mjs && cp dist/reduce.user.css \"${W3STIL_DEST:-.}\""
  },
  "devDependencies": {
    "playwright": "^1.49.0",
    "postcss": "^8.4.49",
    "stylelint": "^16.11.0",
    "stylelint-config-standard": "^36.0.1"
  }
}
